import { tokens } from '../../tokens/fcx.json';
console.log(tokens)
export interface fonts {
  fcx: any;
  body: {
    fontstyle: string;
    fontfamily: string;
    fontsize: number;
    fontweight: number;
    lineheight: number;
  };
}
